# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variáveis de ambiente (NUNCA commitar)
.env
.env.local
.env.*.local
.env.backup*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SingleFile HTML dumps (página completa salva via extensão SingleFile)
# Ficam grandes (>500KB) e são fonte temporária — não devem ser versionados
/*.html
!index.html

# Supabase CLI runtime cache (project-ref, versões etc) — não versionar
supabase/.temp/
